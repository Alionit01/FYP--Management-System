[project]
name = "fyp-finder-backend"
version = "0.1.0"
description = "FYP Finder backend"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "psycopg[binary]",
    "python-dotenv",
    "email-validator",
    "passlib[bcrypt]",
    "python-jose[cryptography]"
]